<?xml version="1.0" encoding="GBK" ?>
<TRANSACTION RIGHTS="11111111011100000000000001100010" TITLE="1826      下载当日成功交易数据)"
	xmlns="http://www.otd.org/MENU" xmlns:W3CNS="http://www.w3c.org">
<COMMSEND>
"{
		refresh();
	  initfd();
	  commsend_001();
}"
</COMMSEND>
<COMMRECV>
	"{
	}"
</COMMRECV>

<VARIABLE>
	<ITEM LINE="3" COL="3" TYPE="%-54s" FUNCTIONS="T(1826                      下载当日成功交易数据)"/>
	<ITEM LINE="4" COL="3" TYPE="%-74s" FUNCTIONS="T(─────────────────────────────────────)"/>
	<ITEM NAME="#TXNAME"   TYPE="%-30s"  FUNCTIONS="T(下载当日成功交易数据)"/>
	<!--ITEM NAME="#DOLIST" TYPE="%1s" FUNCTIONS="T()">
	<ONLOSTFOCUS>
	"{
		  dim @selid as number;
		  if(#DOLIST=='1')
		  {
			    @selid=list_work();     
			    if(@selid &lt; 0)       
			    {                     
			    	  list_del();            
			    	  rerun();               
			    	  return(true);          
			    }           
			    @selid=list_getselid();
			    if(@selid &lt; 0)
			    {
			    	  list_del();
			    	  rerun();
			    	  return(true);
			    }
		  }
		  #DOLIST='';
		  list_del();
	}"
	</ONLOSTFOCUS>
	</ITEM-->
	<ITEM LINE="8" COL="29" TYPE="%-32s" FUNCTIONS="T(是否下载当日成功交易数据)"/>
	<ITEM  NAME="#OK" LINE="21"  COL="63"  TYPE="%-6s"   FUNCTIONS="T(确  定)" INPUT="BUTTON">
 	  <ONCLICK>
 		 "{
				dim @buffer as string; 
				initfd();
				commsend_001();
				$FD16 = '2303'; 
				$FD18 = 'pbp';
				$FD28 = 'sxps';
				
				//dim @rcvbuffer as string;
				
				callagnpbp();
				if($FD12!='0000')
				{
						showmsg(geterror());
						return(false);
				}
				@buffer=getfiletxt('../tmp/'+$KINBR+$TTYNAME);
				if(@buffer == '')
				{
					showmsg('未找到记录!');
					return(false);
				}
				if(autolist('~'+@buffer))
				{ 					 
				    list_show();
				}     
				runoutput();		
				return (true);	
				rerun();
				refresh();
 		 }"
 	  </ONCLICK>  
 	</ITEM>   
</VARIABLE>
<RESPONSE>
<PACKAGE DEVICE="PRINTER"  LINESPACE="24" DESC="按任意键退出">
NOTHING
</PACKAGE>
<PRINT>
	"{       
		list_print();
	}" 
</PRINT>
</RESPONSE>
</TRANSACTION>
